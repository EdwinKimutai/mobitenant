import { StyleSheet } from "react-native";

const style = StyleSheet.create({
  container: {
    width: "100%",
    textAlign: "center",
  },
  scrollContainer: {
    textAlign: "center",
  },
});
export default style;
